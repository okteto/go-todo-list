build:
  go-todo-list:
    image: okteto.dev/go-todo-list:001

deploy:
  - kubectl apply -k manifests

dev:
  go-todo-list:
    image: okteto/golang:1
    command: bash
    securityContext:
      capabilities:
        add:
        - SYS_PTRACE
    volumes:
    - /go/pkg/
    - /root/.cache/go-build/
    workdir: /app
    sync:
    - .:/app
    forward:
    - 2345:2345
