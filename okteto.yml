build:
  todo-list:
    image: okteto.dev/todo-list:001

deploy:
  - cd manifests; kustomize edit set image okteto.dev/todo-list=${OKTETO_BUILD_TODO_LIST_IMAGE}
  - kubectl apply -k manifests

dev:
  todo-list:
    command: bash
    securityContext:
      capabilities:
        add:
        - SYS_PTRACE
    volumes:
    - /go/pkg/
    - /root/.cache/go-build/
    workdir: /app
    sync:
    - .:/app
    forward:
    - 2345:2345
